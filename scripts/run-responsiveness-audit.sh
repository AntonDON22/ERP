#!/bin/bash

# 🔍 Запуск аудита адаптивности интерфейса
# Автоматическая проверка всех React компонентов на соответствие mobile-first стандартам

echo "🔍 Запуск аудита адаптивности интерфейса..."
echo "Проверка поддержки экранов от 320px до 1920px"
echo ""

# Делаем скрипт исполняемым
chmod +x scripts/responsiveness-audit.js

# Запускаем аудит
node scripts/responsiveness-audit.js

exit_code=$?

echo ""
if [ $exit_code -eq 0 ]; then
    echo "✅ Аудит завершен успешно - проблем не найдено"
else
    echo "⚠️ Аудит завершен - обнаружены проблемы адаптивности"
    echo "Рекомендуется исправить найденные проблемы для улучшения UX"
fi

exit $exit_code