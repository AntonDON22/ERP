Техническое задание: Стабильное кеширование данных через React Query

🎯 Цель:

Настроить систему кеширования на базе @tanstack/react-query таким образом, чтобы все данные (товары, документы, остатки, и т.д.) автоматически и гарантированно обновлялись в интерфейсе после любых действий (создание, редактирование, удаление), без необходимости обновлять страницу вручную.

⸻

📌 Общие требования:
	•	В проекте уже используется React Query — требуется донастроить стратегию работы с кешем.
	•	Каждому useQuery должен соответствовать чёткий ключ queryKey.
	•	Каждая мутация (create, update, delete) должна сопровождаться правильной invalidateQueries или setQueryData.
	•	Все случаи некорректного отображения данных (например, товар создали, а он не появился) должны быть устранены за счёт кеш-логики, а не ручного обновления.

⸻

🔧 Что нужно реализовать:

1. Организация ключей queryKey
	•	Все useQuery должны быть написаны с предсказуемыми ключами:
	•	['products'] — список товаров
	•	['product', productId] — конкретный товар
	•	['documents'] — список документов
	•	['document', documentId] — документ по ID
	•	и т.п.

2. Инвалидация после мутаций
	•	После создания, обновления или удаления данных необходимо вызывать:
	•	queryClient.invalidateQueries(['products']) — после создания товара
	•	queryClient.invalidateQueries(['document', id]) — после редактирования документа
	•	Инвалидации должны быть централизованы (см. следующий пункт)

3. Создание утилиты useInvalidate
	•	Создать хук useInvalidate, который предоставляет методы для всех ключевых сущностей:
	•	invalidate.products()
	•	invalidate.documents()
	•	invalidate.document(id)
	•	invalidate.contractors() и т.п.
	•	Использовать этот хук во всех мутациях вместо дублирования invalidateQueries вручную

4. Опционально: оптимистичное обновление через setQueryData
	•	Если объект создаётся и уже известен (например, товар), можно использовать queryClient.setQueryData вместо повторного запроса.

5. Гарантировать полную реактивность
	•	После любой операции (создание, изменение, удаление) пользователь должен видеть актуальное состояние сразу.
	•	Не должно быть ситуации, когда нужно вручную обновлять страницу.

⸻

📈 Дополнительные настройки:
	•	Включить refetchOnWindowFocus: true, чтобы данные обновлялись при возвращении во вкладку.
	•	Проверить staleTime и cacheTime — они должны быть логично подобраны, например:
	•	staleTime: 0 для частых обновлений
	•	cacheTime: 5 * 60 * 1000 для временного хранения

⸻

📓 Результат выполнения:
	•	Кеш работает стабильно, без «провалов» и багов
	•	После создания или изменения данных в любой части системы — интерфейс обновляется автоматически
	•	Больше нет необходимости обновлять страницу вручную
	•	Код стал чище, так как инвалидации централизованы
	•	Пользователь всегда видит актуальные данные

⸻

Если нужно, можно также реализовать утилиту логирования кеша (logQueryChanges) или визуализировать кеш-состояние во внутренней панели (например, через ReactQueryDevtools).