Техническое задание

Тема: Унификация числовых полей в API — устранение ошибок от строковых значений, приходящих с фронтенда

⸻

🎯 Цель:

Обеспечить надёжную работу API и схем валидации, которые ожидают числовые значения (price, quantity, amount и т.п.), в условиях, когда данные с фронтенда поступают как строки. Устранить ошибки валидации без необходимости ручного преобразования значений во всех компонентах.

⸻

📌 Проблема:

Во всех HTML-формах и input-элементах, даже с типом number, значения по умолчанию передаются как строки. При этом валидационные схемы на backend (Zod, TypeScript) ожидают тип number, и возникают ошибки, например:
	•	строка "1000" вместо числа 1000
	•	поле "0" интерпретируется как строка и не проходит .nonnegative()

⸻

🛠 Задачи:

1. Создать универсальные числовые схемы с предварительной обработкой данных

Необходимо реализовать набор предобработанных схем, которые:
	•	при получении строки автоматически преобразуют её в число
	•	выбрасывают ошибку, если значение невозможно привести к числу
	•	используются во всех ключевых местах API

Обязательные поля:
	•	zPrice — для полей типа price, purchasePrice
	•	zQuantity — для quantity, reserved, available
	•	zPercent — для скидок, наценок и т.д.

2. Подключить эти схемы во все соответствующие Zod-валидации

В схемах создания и обновления:
	•	продуктов
	•	документов
	•	строк документов
	•	поставщиков, если есть числовые поля

Текущие поля, оформленные как z.number(), должны быть заменены на универсальные схемы.

3. Обеспечить русскоязычные сообщения об ошибке при некорректных значениях

Если пользователь вводит нечисловое значение, API должен возвращать:
	•	“Поле должно быть числом”
	•	“Введите корректное значение цены”

Это улучшит UX и снизит непонимание при работе с системой.

4. Протестировать работу API с формами, передающими строки

Проверить, что форма, отправляющая "100" вместо 100, проходит валидацию. Также протестировать случаи с "0", "", "abc" и undefined.

⸻

🧪 Ожидаемый результат:
	•	Поля с числовыми значениями (цены, количество и т.п.) стабильно обрабатываются API
	•	Значения из форм, передающиеся как строки, корректно приводятся к числам без ошибок
	•	Валидация работает предсказуемо как при ручном вводе, так и в тестах
	•	Все фронтенд-формы работают без необходимости ручного преобразования типов

⸻

📝 Дополнительно:
	•	После реализации схемы можно переиспользовать и в react-hook-form через zodResolver
	•	Это снизит риски багов при работе с большими формами и массовыми операциями
	•	При желании можно вынести схемы в файл shared/zFields.ts и использовать их глобально

⸻

Результат выполнения этого ТЗ — устранение целого класса ошибок, связанных с несовпадением типов между фронтом и API, и повышение стабильности работы ERP-системы.